#!/usr/bin/env bash
cat <<'JSON'
{"hookSpecificOutput":{"hookEventName":"UserPromptSubmit","additionalContext":"DIRECTIVE: For ConfigHub or Kubernetes-on-ConfigHub work, invoke the single best matching confighub-skills skill before acting. This 0.4.0 companion is knowledge/read-only: it may inspect, teach, and prepare an exact proposal, but it MUST NOT execute create/update/approve/promote/publish/withdraw/install/delete operations because the external mutation broker is NOT_INTEGRATED. A chat confirmation or ConfigHub revision approval is not execution authorization. Confirm verbs and flags with installed cub help. Route: confighub-core (orientation/topology/doctrine), kubernetes-resources (resource proposal), app-config (AppConfig/ConfigMap proposal), cub-query (reads), cub-mutate (mutation proposal), triggers-and-applygates (policy/gates/native revision approval), worker-bootstrap (worker proposal/diagnosis), target-bind (OCI ReleaseTargetID + Unit binding proposal), release-publish (exact whole-Space Release/read/withdraw proposal), verify-apply (Release/controller/runtime proof), import (Helm/Kustomize Component/Variant onboarding proposal), promote-release (promotion/approval proposal), rollback-revision (head-moving rollback proposal), incident-management (read-only incident orchestration), skill-examples-bootstrap (playground proposal). Bridge/per-Unit and ConfigHub-provider delivery are VERSIONED_LEGACY/BLOCK in the reviewed profile. If intent is unclear, invoke confighub-core."}}
JSON
