#!/usr/bin/env bash
cat <<'JSON'
{"hookSpecificOutput":{"hookEventName":"UserPromptSubmit","additionalContext":"DIRECTIVE: For ConfigHub or Kubernetes-on-ConfigHub work, invoke the single best matching confighub-skills skill before acting and follow references/execution-modes.md. The skills are standalone by default; a stricter external overlay is optional. allowed-tools is empty, so this pack preapproves nothing. When the user clearly requests a change, resolve and briefly state the exact scope, then make one Bash tool call containing one exact mutation command and submit it to the host permission system, which may prompt, allow, or deny it. Do not require a second approval system and do not hand the command back for terminal execution unless the host denies it, Bash is unavailable, or the session is explicitly coached. If a separately installed governance overlay explicitly blocks or adds a gate, obey that external policy before Bash. Confirm verbs and flags with installed cub help. Route: confighub-core (orientation/topology/doctrine), kubernetes-resources (resource authoring), app-config (AppConfig/ConfigMap), cub-query (reads), cub-mutate (Unit changes), triggers-and-applygates (policy/gates/native revision approval), worker-bootstrap (worker setup/diagnosis), target-bind (OCI ReleaseTargetID + Unit binding), release-publish (whole-Space Release/read/withdraw), verify-apply (Release/controller/runtime proof), import (Helm/Kustomize Component/Variant onboarding), promote-release (promotion), rollback-revision (head-moving rollback), incident-management (incident orchestration), skill-examples-bootstrap (playground). Historical bridge/per-Unit delivery and direct ConfigHub-provider delivery are unsupported in the reviewed current profile. If intent is unclear, invoke confighub-core."}}
JSON
