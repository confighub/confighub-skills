#!/bin/bash
cat <<'JSON'
{"hookSpecificOutput":{"hookEventName":"UserPromptSubmit","additionalContext":"DIRECTIVE: If this prompt involves ConfigHub or Kubernetes-on-ConfigHub work, you MUST invoke the most suitable skill from the confighub-skills plugin before doing anything else. Do not propose, suggest, or describe a skill — invoke it. Do not compose cub commands from memory. Pick the single best match from this routing table and invoke it now: confighub-core (orientation, routing, Delete/Destroy gates), config-as-data (authoring doctrine), kubernetes-resources (author K8s resources as Units), app-config (ConfigMap/.env/.properties/.yaml), space-topology (Space layout per env/region), cub-query (read-only query across Units/Spaces/Revisions), cub-mutate (bulk + surgical Unit mutation), triggers-and-applygates (validation policy + ApplyGates), worker-bootstrap (install bridge workers), target-bind (create Targets, bind Units), cub-apply (apply Units to Targets), verify-apply (post-apply verification), import-from-helm, import-from-kustomize, import-from-argocd, import-from-flux, import-from-cluster, import-unit-granularity (one Unit or many?), promote-release (env-by-env or fleet promotion), rollback-revision, drift-reconcile (ConfigHub vs cluster divergence), incident-management (orchestrated incident plan). If the intent is unclear, invoke confighub-core to route. Fall back to cub --help only when zero skills match.\n\nFUNCTION USAGE: Never guess function names or argument shape — functions are discoverable via the cub CLI. Before calling any function, (1) enumerate with `cub function list --names --toolchain Kubernetes/YAML` (scope with --space/--unit/--target/--worker when relevant), then (2) verify the signature with `cub function explain <name>` — it prints the literal `Usage: cub function do -- <name> <args>` line plus required parameters, data types, constraints, and an example. Invoke with `cub function do -- <name> <args>` (the `--` stops cub flag parsing). General-purpose YAML escape hatches exist and should be used when no typed function matches: `yq` for read-only queries (non-mutating, returns a result; takes a single yq expression like `.spec.replicas`), and `yq-i` for in-place YAML mutation. Prefer typed functions (e.g. `set-container-image`, `set-replicas`, `set-namespace`, `set-env-var`, `set-annotation`, `set-label`, `set-container-resources`, `vet-schemas`, `vet-cel`) over `yq`/`yq-i` when one fits — they carry stronger validation and intent."}}
JSON
