{
  "execution_policy": "PROPOSE_ONLY_UNTIL_EXTERNAL_BROKER",
  "prompts": [
    {
      "name": "statefulset-for-redis",
      "prompt": "I need to deploy a Redis cluster as a StatefulSet in ConfigHub with 3 replicas and persistent storage. The Redis pods should use the redis:7.2 image and listen on port 6379. Set it up in my redis-prod Space.",
      "expected_output": "Authors literal StatefulSet plus required headless Service/PVC-template resources with security, probes, requests/limits, and exact namespace/storage questions. Maps one resource per Unit, prepares governed Unit/link proposals with provenance, never creates them, and hands eventual delivery to release-publish after broker approval."
    },
    {
      "name": "lock-down-namespace-networking",
      "prompt": "I want to lock down networking in my app namespace. Default deny everything, then allow my frontend pods to talk to backend pods on port 8080, and backend to reach the database on port 5432. All pods need DNS. How do I set this up in ConfigHub?",
      "expected_output": "Authors explicit ingress/egress NetworkPolicies including namespace DNS, validates selectors/namespaces/IP-family assumptions, and explains default-deny consequences. Produces one-resource-per-Unit proposals and a staged proof plan; does not apply or create resources."
    },
    {
      "name": "cronjob-with-rbac",
      "prompt": "I need a CronJob that runs every hour to clean up expired sessions from our database. It needs a ServiceAccount with permission to read Secrets (just the one named db-credentials) and ConfigMaps. The cleanup script is in our image at ghcr.io/myorg/session-cleanup:v2.1.0. Put it in the myapp-prod Space.",
      "expected_output": "Authors separate ServiceAccount, Role, RoleBinding, and CronJob proposals with least privilege/resourceNames for db-credentials, sensible concurrency/history/security settings, and external-secret cautions. Uses exact image, one resource per Unit, does not execute, and includes review/proof steps."
    }
  ]
}
